package collections

var iQ IntQueue
var iQueue = iQ.New(6)

// IntQueue testss
//---------------------------------------------------------------------------------------------------- <-100
/*
func TestQPop(t *testing.T) {
	t.Log("Testing: IntSlice for Pop")
	iQueue.Put(5)
	v, ok := iQ.Pop()
	if v != 1 && ok == false {
		t.Error("Pop() not expected value of 1: " + strconv.Itoa(v))
	}
	if iQueue.Content.Len() != 4 {
		t.Error("Pop() not expected value of 4: " + strconv.Itoa(iQueue.Content.Len()))
	}
}

func TestQBuffer(t *testing.T) {
	t.Log("Testing: negative values IntSlice for Max")
	v, ok := iQ.Buffer(5)
	if v != -1 && ok == false {
		t.Error("Max() not expected value of 1: " + strconv.Itoa(v))
	}
	if iQueue.Content.Len() != 5 {
		t.Error("Max() not expected value of 4: " + strconv.Itoa(iQueue.Content.Len()))
	}
}
*/
